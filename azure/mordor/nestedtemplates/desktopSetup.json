{
    "$schema": "https://schema.management.azure.com/schemas/2015-01-01/deploymentTemplate.json#",
    "contentVersion": "1.0.0.0",
    "parameters": {
        "vmList": {
            "type": "string",
            "minLength": 1,
            "metadata": {
                "description": "List of virtual machines to run powershell scripts, if using multiple VMs, make their names comma separate. E.g. VM01, VM02, VM03."
            }
        },
        "extensionName": {
            "type": "string",
            "metadata": {
                "description": "name of the extension"
            }
        },
        "fileUris": {
            "type": "array"
        },
        "commandToExecute": {
            "type": "string"
        },
        "location": {
            "type": "string",
            "metadata": {
                "description": "Location for all resources."
            }
        }
    },
    "variables": {
        "vmListArray": "[split(parameters('vmList'),',')]"
    },
    "resources": [
        {
            "type": "Microsoft.Compute/virtualMachines/extensions",
            "apiVersion": "2016-08-30",
            "name": "[concat(trim(variables('vmListArray')[copyIndex()]), parameters('extensionName'))]",
            "location": "[parameters('location')]",
            "copy": {
                "name": "vmPrepareBoxCopy",
                "count": "[length(variables('vmListArray'))]"
            },
            "properties": {
                "publisher": "Microsoft.Compute",
                "type": "CustomScriptExtension",
                "typeHandlerVersion": "1.8",
                "autoUpgradeMinorVersion": true,
                "settings": {
                    "fileUris": [
                        "[parameters('fileUris')]"
                    ],
                    "commandToExecute": "[parameters('commandToExecute')]"
                }
            }
        }
    ]
}